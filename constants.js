// constants that I can use inside of my codes

const NETWORK = {
  IP: 'localhost',
  PORT: 50541
};

const KEY_BINDINGS = {
  MOVE_UP: 'w',
  MOVE_LEFT: 'a',
  MOVE_DOWN: 's',
  MOVE_RIGHT: 'd'
};

const MESSAGES = {
  z: 'Hey',
  x: 'Nice',
  c: 'OH NO'
};

module.exports = {
  NETWORK,
  KEY_BINDINGS,
  MESSAGES
};